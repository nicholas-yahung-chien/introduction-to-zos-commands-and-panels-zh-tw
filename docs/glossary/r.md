# IBM Z 詞彙表：R

[返回詞彙表索引](./)

## Record format (RECFM)

資料集記錄格式。課程配置題中的 `RECFM(F,B)` 表示 fixed blocked records。

## RFIND command

Repeat FIND。ISPF 中用來重複上一個 FIND 搜尋的命令，常對應到 function key `F5`。

## REXX

z/OS 上常見的程序語言之一，可在 TSO/E 與 ISPF 環境中自動化命令或建立互動工具。
